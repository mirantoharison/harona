const getApiUrl = () => {
  // Utilisez `window` pour accéder à des variables globales, ou remplacez par des valeurs d'environnement lors du build.
  return "https://harona-eight.vercel.app/"; // valeur par défaut pour dev
};

export default getApiUrl;